export enum SharedEmitConstants {
    // game
    SEND_GAME_DATA,
    GAME_MESSAGE,
    GAME_CLICKED_POINT_INDEX,

    // rooms
    ROOM_CREATE,
    ROOM_JOIN,
    ROOM_LEAVE,
    ROOM_LIST_INFOS,

    // system
    USER_INFO,
    ERROR,
}
