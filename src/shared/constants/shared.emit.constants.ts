export enum SharedEmitConstants {
    // rooms
    ROOM_CREATE,
    ROOM_JOIN,
    ROOM_LEAVE,
    ROOM_LIST_INFOS,
    ROOM_ERROR,

    // system
    USER_INFO,

    // game
    GAME_ERROR,
    SEND_GAME_DATA,
    GAME_CLICKED_POINT_INDEX,
    GAME_REQUEST_REMATCH,
}
