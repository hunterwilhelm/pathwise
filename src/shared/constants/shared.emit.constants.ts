export enum SharedEmitConstants {
    // rooms
    ROOM_CREATE,
    ROOM_JOIN,
    ROOM_LEAVE,
    ROOM_LIST_INFOS,

    // system
    USER_INFO,
    ERROR,

    // game
    SEND_GAME_DATA,
    GAME_CLICKED_POINT_INDEX,
    GAME_REQUEST_REMATCH,
}
