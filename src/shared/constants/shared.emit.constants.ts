export enum SharedEmitConstants {
    // game
    GAME_MESSAGE,

    // rooms
    ROOM_CREATE,
    ROOM_JOIN,
    ROOM_LEAVE,
    ROOM_LIST_INFOS,
    GAME_CLICKED_POINT_INDEX,

    // system
    USER_INFO,
    ERROR,
}
